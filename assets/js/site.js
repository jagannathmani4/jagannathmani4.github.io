import { firebaseConfig, FIRESTORE_DOC_PATH, PROFILE_IMAGE_URL } from "./config.js";
const defaults={projects:[{title:"Excel Sales & Orders Dashboard",description:"Interactive sales and order analysis dashboard built in Microsoft Excel.",software:["Microsoft Excel","PivotCharts","Data Analysis"],link:"assets/projects/files/excel%20task.xlsx",image:"assets/projects/images/Screenshot%202026-09-04%20115031.png"}],skills:[{name:"JavaScript / React",level:"Building"},{name:"Node.js / PHP",level:"Building"},{name:"Python / Machine Learning",level:"Exploring"},{name:"UI & Interaction",level:"Always curious"}]};
const $=id=>document.getElementById(id); document.querySelectorAll(".avatar-wrap img,.mobile-avatar,.hero-avatar").forEach(image=>image.src=PROFILE_IMAGE_URL); const escape=value=>String(value||"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]));
function render(data=defaults){const projects=data.projects||defaults.projects;$ ("projectGrid").innerHTML=projects.length?projects.map((p,i)=>`<article class="project-card"><span class="number">PROJECT</span>${p.image?`<img class="project-image" src="${escape(p.image)}" alt="${escape(p.title)} dashboard preview">`:""}<div><h4>${escape(p.title)}</h4><p>${escape(p.description||p.software||"")}</p></div><div class="tags">${(p.tags||p.software||[]).map(t=>`<span>${escape(t)}</span>`).join("")}${p.link?`<a href="${escape(p.link)}" download>Download file ↗</a>`:""}</div></article>`).join(""):"<p class=\"empty-projects\">Projects are on the way. Check back soon.</p>";$("skillList").innerHTML=(data.skills||defaults.skills).map(s=>`<div class="skill"><span>${escape(s.name)}</span><small>${escape(s.level)}</small></div>`).join("");}
render(); $("year").textContent=new Date().getFullYear(); $("menuOpen").addEventListener("click",()=>$("sidebar").classList.add("open")); $("sidebarClose").addEventListener("click",()=>$("sidebar").classList.remove("open")); document.querySelectorAll(".side-nav a").forEach(a=>a.addEventListener("click",()=>$("sidebar").classList.remove("open")));
if(!firebaseConfig.apiKey.startsWith("YOUR_")){import("https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js").then(async({initializeApp})=>{const {getFirestore,doc,getDoc}=await import("https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js");const snap=await getDoc(doc(getFirestore(initializeApp(firebaseConfig)),FIRESTORE_DOC_PATH.collection,FIRESTORE_DOC_PATH.doc));if(snap.exists())render({...defaults,...snap.data()});}).catch(error=>console.warn("Firestore content unavailable; using local content.",error));}
const sections=[...document.querySelectorAll("main section[id]")],navLinks=[...document.querySelectorAll(".side-nav a")];const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("visible");navLinks.forEach(link=>link.classList.toggle("active",link.getAttribute("href")==="#"+entry.target.id));}}),{threshold:.08,rootMargin:"-20% 0px -55% 0px"});sections.forEach(section=>{section.classList.add("reveal");observer.observe(section);});
document.querySelectorAll(".project-card").forEach(card=>{card.addEventListener("click",()=>card.classList.toggle("expanded"));card.addEventListener("mousemove",event=>{const box=card.getBoundingClientRect();card.style.setProperty("--tilt-x",`${(event.clientY-box.top)/box.height*4-2}deg`);card.style.setProperty("--tilt-y",`${2-(event.clientX-box.left)/box.width*4}deg`);});card.addEventListener("mouseleave",()=>{card.style.removeProperty("--tilt-x");card.style.removeProperty("--tilt-y");});});
if(window.matchMedia("(pointer:fine)").matches)window.addEventListener("pointermove",event=>{document.documentElement.style.setProperty("--mouse-x",`${event.clientX}px`);document.documentElement.style.setProperty("--mouse-y",`${event.clientY}px`);});